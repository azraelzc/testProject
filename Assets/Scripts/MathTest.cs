using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MathTest : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Debug.LogError(Mathf.Lerp(1,5,0.5f));
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
